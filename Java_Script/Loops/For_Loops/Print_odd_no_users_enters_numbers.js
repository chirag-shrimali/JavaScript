// printing odd numbers between 1 to 100

console.log("\nThe Odd numbers between 1 to 100 are : ");

for(let i = 1;i<=100;i++)
{
    if(i % 2 != 0)
    {
       console.log(" " + i);
    }
}
    // console.log(" " + oddno);